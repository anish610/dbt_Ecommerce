[project]
name = "dbt-ecom"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Anish Shetty", email = "anish610@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "dbt-core>=1.12.3",
    "dbt-databricks>=1.12.5",
]

[project.scripts]
dbt-ecom = "dbt_ecom:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
